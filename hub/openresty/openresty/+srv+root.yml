version: '3.1'

# makes snippet resty root available

services:
  resty:
    volumes:
    - ${RESTY_VOLUMES_SRV:-../SITES}:/srv:rw${MOUNT_OPTIONS}
    - ./hub/openresty/openresty/conf/resty-root:/usr/local/openresty/nginx/conf/resty-root:ro${MOUNT_OPTIONS}


