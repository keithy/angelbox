version: '3.1'

services:
  resty:
    volumes:
    - ${RESTY_VOLUMES_MY_SNIPPETS:-./hub/openresty/openresty/my-snippets}/nginx.conf:/usr/local/openresty/nginx/conf/nginx.conf:ro${MOUNT_OPTIONS}
    - ${RESTY_VOLUMES_MY_SNIPPETS:-./hub/openresty/openresty/my-snippets}:/usr/local/openresty/nginx/conf/my:ro${MOUNT_OPTIONS}


