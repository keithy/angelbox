version: '3.1'

services:
  redis:
    volumes:
      - ${SECRETS}:/run/secrets:ro${MOUNT_OPTIONS}
    command: >
      --requirepass $(cat /run/secrets/redis_password)
      --appendonly ${REDIS_COMMAND_APPENDONLY:-yes}
